# Doni 网站上线手把手教学

写给不碰命令行的人。命令都可以直接复制粘贴,不用理解。

---

## 0. 先搞清楚三件事

**网站文件在哪**

```
/Users/mac/Documents/Claude/Projects/doni-site/
├── index.html          ← 整个网站就是这一个文件(所有文字都在里面)
└── assets/
    └── doni-qr.png     ← WhatsApp 二维码图片
```

就这两个文件。没有数据库、没有服务器、没有需要安装的东西。**整个网站 = 一个文件夹**,把这个文件夹放到任何能放文件的地方,网站就上线了。

**为什么用 Cloudflare Pages**

- 免费,静态网站基本不可能超额
- 自动送 HTTPS 证书(小绿锁),不用管
- 你的域名本来就在 Cloudflare,绑域名是点两下的事
- 全球 CDN,拉各斯和国内打开都快

**两条路选一条**

| | A 路:GitHub + 自动部署 | B 路:直接拖文件夹 |
|---|---|---|
| 上线要多久 | 10 分钟 | 3 分钟 |
| 要用命令行吗 | 要复制粘贴 4 行 | 完全不用 |
| 以后改内容 | 改完 push,自动更新 | 每次重新拖一遍文件夹 |
| 推荐给谁 | **推荐,一次麻烦长期省事** | 想今天就看到网站 |

**拿不定主意就走 B 路先上线,以后随时能换成 A 路**(换的时候网址和域名都不用动)。

---

## A 路:GitHub + Cloudflare Pages 自动部署

### A1. 把代码推到 GitHub

打开「终端」(Terminal,在 Mac 的「启动台 → 其他」里),把下面这段**整段**复制进去,回车:

```bash
cd /Users/mac/Documents/Claude/Projects/doni-site
git add -A
git commit -m "网站内容更新"
gh repo create doni-site --private --source=. --remote=origin --push
```

> 注意:你本地现在有一处**还没保存的改动**,上面第 2、3 行就是在保存它,别跳过。

跑完最后一行会打印一个网址,像 `https://github.com/susanchensusan/doni-site`。看到它就说明成功了。

- 如果提示 `Name already exists`:说明这个仓库名被占了,把命令里的 `doni-site` 改成 `doni-site-2` 再跑一次。
- `--private` 表示仓库是私有的,只有你能看见。网站本身照样是公开的,不冲突。

### A2. 在 Cloudflare 建 Pages 项目

1. 打开 https://dash.cloudflare.com,登录。
2. 左边菜单点 **Workers & Pages**(有的账号显示成 **Compute**)。
3. 右上角蓝色按钮 **Create** → 顶部切到 **Pages** 这个页签 → 选 **Connect to Git**。
4. 第一次用会跳到 GitHub 问你授权:
   - 页面标题写着 *Install Cloudflare Pages*
   - 选 **Only select repositories** → 下拉里勾上 `doni-site`
   - 点绿色 **Install & Authorize**,可能要输一次 GitHub 密码
5. 回到 Cloudflare,列表里点 `doni-site` → 点 **Begin setup**。

### A3. 构建设置(最关键的一屏,别填错)

这一屏叫 *Set up builds and deployments*,照抄:

| 栏位 | 填什么 |
|---|---|
| Project name | `doni-site`(这个名字会变成临时网址,后面还能改) |
| Production branch | `main` |
| Framework preset | **None** |
| Build command | **留空,什么都别填** |
| Build output directory | `/` (一个斜杠) |

> 我们的网站不需要「构建」,直接就是成品。所以 Build command 必须空着 —— 填了反而会报错。

点 **Save and Deploy**。

### A4. 等 30 秒

会看到一个黑色滚动的日志窗口,最后出现 **Success! Your site is live** 和一个网址:

```
https://doni-site.pages.dev
```

点开它 —— 网站已经在线上了,而且已经有 HTTPS。**先在这个临时网址上确认页面正常,再去绑域名。**

到这里 A 路完成。跳到「绑定域名」一节。

---

## B 路:直接拖文件夹上传(3 分钟,不碰命令行)

1. 打开 https://dash.cloudflare.com,登录。
2. 左边菜单 **Workers & Pages** → 右上角 **Create** → 顶部切到 **Pages** 页签 → 这次选 **Upload assets**(不是 Connect to Git)。
3. Project name 填 `doni-site` → 点 **Create project**。
4. 出现一个虚线框,里面写着 *Drag and drop your site files here* 和一个 **Select from computer** 按钮。
5. 打开访达(Finder),按 `Command + Shift + G`,粘贴这个路径回车:

   ```
   /Users/mac/Documents/Claude/Projects/doni-site
   ```

6. **把 `index.html` 和 `assets` 文件夹这两个一起选中,拖进虚线框**。

   ⚠️ 关键:拖的是**文件夹里面的内容**,不是 `doni-site` 这个文件夹本身。拖错了网站会打不开(因为 index.html 跑到了二级目录)。上传完虚线框下方应该列出 `index.html`,而不是 `doni-site/index.html`。

7. 点 **Deploy site**,等十几秒,看到网址:

   ```
   https://doni-site.pages.dev
   ```

点开确认页面正常。B 路完成。

---

## 绑定域名

`.pages.dev` 那个网址能用,但不好看。下面把自己的域名接上去。

前提:域名已经在你的 Cloudflare 账号里(在 dash 首页能看到它,状态是 **Active**)。

> 提醒:`sco.com.ng` 之前 zone 被删过。如果 dash 里找不到它,先在 Cloudflare 首页点 **Add a site** 把它加回来,再去域名注册商那里把 NS(名称服务器)改成 Cloudflare 给的两条,等状态变 Active 再往下做。

### 主域名接上去(3 步)

1. Cloudflare → **Workers & Pages** → 点你的项目 `doni-site`。
2. 上方页签点 **Custom domains** → 蓝色按钮 **Set up a custom domain**。
3. 输入框里填你要用的域名,比如:

   ```
   fctech.ng
   ```

   (想用二级域名就填 `doni.fctech.ng`,一样的做法)

   点 **Continue** → 页面会显示它将要自动添加的 DNS 记录(一条 CNAME 指向 `doni-site.pages.dev`)→ 点 **Activate domain**。

因为域名就在同一个 Cloudflare 账号里,**DNS 记录它自己会加,你不用手动填任何东西**。

状态会先显示 **Initializing**,几分钟后变成绿色的 **Active**。变绿之后打开 `https://fctech.ng` 就是你的网站,HTTPS 自动生效。

### 顺手把 www 也加上

重复上面 3 步,这次填 `www.fctech.ng`。这样两种写法都能打开。

### 「我有一堆域名,想都指向这个网站」

**不要**给每个域名都做 Custom domain 或者 CNAME —— 那样搜索引擎会认为你有好几个一样的网站,而且用户看到的地址栏是乱的。

**正确做法:选一个主域名(比如 `fctech.ng`),其余域名全部 301 永久跳转到它。** 好处是权重集中、地址栏永远显示主域名、以后改主站只改一处。

下面以「把 `fctech.online` 跳到 `fctech.ng`」为例,**每个要跳转的域名都做一遍**。

#### 第 1 步:给这个域名加一条「占位」DNS 记录(不能跳过)

跳转规则只对「经过 Cloudflare 的流量」生效。如果这个域名没有任何 DNS 记录,请求根本到不了 Cloudflare,规则就不会触发。所以先加一条假记录:

1. Cloudflare 首页 → 点进 `fctech.online`
2. 左边菜单 **DNS** → **Records** → **Add record**
3. 填:

   | 栏位 | 填什么 |
   |---|---|
   | Type | `AAAA` |
   | Name | `@` |
   | IPv6 address | `100::` |
   | Proxy status | **点一下让云朵变成橙色 Proxied** ← 最关键 |

   点 **Save**。

4. 再加一条一模一样的,只把 Name 改成 `www`。

> `100::` 是一个「黑洞地址」,专门用来做这种占位。云朵必须是**橙色**,灰色的话跳转不会生效。

#### 第 2 步:建 301 跳转规则

1. 还在 `fctech.online` 这个域名里,左边菜单 **Rules** → **Redirect Rules**(有的界面是 Rules → Overview → **Create rule** → 选 **Redirect Rule**)
2. 点 **Create rule**,照下面填:

**Rule name(规则名,随便起):**
```
redirect to fctech.ng
```

**When incoming requests match(什么时候触发):**

选 **All incoming requests**(所有请求都跳)。

> 如果这里没有 "All incoming requests" 选项,就选 Custom filter expression,然后:Field 选 `Hostname`,Operator 选 `contains`,Value 填 `fctech.online`。效果一样。

**Then...(跳到哪):**

| 栏位 | 选/填什么 |
|---|---|
| Type | **Dynamic**(动态,不是 Static) |
| Expression | `concat("https://fctech.ng", http.request.uri.path)` |
| Status code | **301**(Permanent Redirect) |
| Preserve query string | ✅ **打勾** |

3. 点 **Deploy**。

> **为什么用 Dynamic 而不是 Static?**
> Static 的话,不管别人访问 `fctech.online/abc` 还是 `fctech.online/xyz`,统统甩到首页。
> Dynamic 用上面那行 `concat(...)`,访问 `fctech.online/abc` 会跳到 `fctech.ng/abc`,路径保留 —— 以后网站加了子页面也不会跳错。现在网站只有一个首页,看不出区别,但这么填以后不用回来改。

#### 第 3 步:验证

浏览器无痕窗口打开 `http://fctech.online`,地址栏应该瞬间变成 `https://fctech.ng`。

其他域名(`sco.com.ng` 等)重复第 1、2 步,`concat` 里的目标地址都写主域名。

---

## 以后怎么改内容

### 要改的文字在哪

**全部文字都在 `index.html` 这一个文件里。** 用「文本编辑」或者 VS Code 打开:

```
/Users/mac/Documents/Claude/Projects/doni-site/index.html
```

改文字就是直接找到那句话改掉,不要动 `<` `>` 里面的东西。二维码换图就把新图片存成同名的 `assets/doni-qr.png` 覆盖掉。

改完先**双击 index.html 在浏览器里看一眼**,确认没改坏,再往上发。

### A 路的人:改完跑这一段

```bash
cd /Users/mac/Documents/Claude/Projects/doni-site
git add -A
git commit -m "改了首页文案"
git push
```

回车之后什么都不用管。Cloudflare 会在大约 30 秒内自动重新部署,线上就更新了。(引号里那句话随便写,是给自己看的备注。)

想确认有没有部署成功:Cloudflare → Workers & Pages → `doni-site` → **Deployments**,最上面一条应该是刚刚的时间,状态 **Success**。

### B 路的人:改完重新拖一次

1. Cloudflare → Workers & Pages → `doni-site`
2. 上方页签 **Deployments** → 右上角 **Create deployment**
3. 和第一次一样:选中 `index.html` + `assets` 拖进去 → **Deploy site**

域名不用重新绑,自动指向最新版本。

---

## 排错

### 域名接上去了,打不开 / 一直转圈

1. **先等 5 分钟**。DNS 生效需要时间,尤其刚改过 NS 的域名。
2. 检查项目里 **Custom domains** 那一栏的状态是不是绿色 **Active**。如果卡在 **Initializing** 超过 15 分钟,点右边的 `...` → **Remove**,然后重新加一遍(80% 的情况这样就好了)。
3. 检查这个域名在 Cloudflare 首页的状态是不是 **Active**。如果写着 **Pending Nameserver Update**,说明 NS 还没指过来 —— 去域名注册商后台把名称服务器改成 Cloudflare 给的那两条。
4. 用无痕窗口再试一次,排除是自己浏览器的缓存问题。

### 打开提示「不安全」/ HTTPS 证书没签发

- Cloudflare 签证书通常几分钟,**最长可能要 24 小时**,先等。
- 去域名的 **SSL/TLS → Overview**,加密模式必须是 **Full** 或 **Full (strict)**。如果是 **Flexible**,改成 **Full**。
- 去 **SSL/TLS → Edge Certificates**,确认 **Always Use HTTPS** 是打开的。
- 如果卡了一天还不行:把 Custom domain 删掉重新添加一次,会重新触发签发。

### 改了内容,线上还是旧的

按顺序排查:

1. **A 路的人先确认 push 成功了**:Cloudflare → 项目 → **Deployments**,最上面那条的时间是不是刚刚。如果还是老时间,说明 `git push` 没跑成功,回去重跑一遍那段命令。
2. **清自己浏览器的缓存**:Mac 上按 `Command + Shift + R` 强制刷新。或者直接开无痕窗口 —— 如果无痕里是新的,那就纯粹是你自己电脑缓存,别慌。
3. **清 Cloudflare 的缓存**:Cloudflare → 点进你的域名 → 左边 **Caching** → **Configuration** → **Purge Everything** → 确认。等 30 秒再刷新。
4. **手机上看还是旧的**:手机运营商网络缓存更久,开飞行模式再关掉,或者切到 WiFi 再试。

### 网站打开是一片空白 / 图片裂了(B 路专属)

八成是拖文件夹的时候拖成了外层文件夹。回到 **Deployments → Create deployment**,重新拖一次,记住:**选中 `index.html` 和 `assets` 两个一起拖,不要拖 `doni-site` 文件夹本身**。

---

## 上线后立刻做这三件事

### 1. 打开 Cloudflare Web Analytics(2 分钟,免费,不拖慢网站)

知道有多少人来看、从哪来。

Cloudflare → **Workers & Pages** → `doni-site` → **Settings** → 找到 **Web Analytics** → 点 **Enable**。

就这一下,不用改任何代码。之后在左边菜单 **Analytics & Logs → Web Analytics** 里看数据。它不用 cookie,不需要挂隐私声明。

### 2. 在手机上从头到尾看一遍

网站的访客九成用手机。用自己的手机打开 `https://fctech.ng`,重点确认三件事:

- 文字有没有被切掉、按钮是不是好按
- 二维码图片显示正常吗
- **点一下 WhatsApp 那个按钮**,确认能直接跳到和 Doni 的对话框(链接是 `wa.me/2349153264893`,号码错了就白做了)

### 3. 把链接发给自己,再当成陌生人点一遍

在 WhatsApp 里把网址发给自己(或者发到任何一个群里再撤回),然后**从聊天框里点进去**。

这一步是在测真实场景:WhatsApp 会自己抓取网页标题和摘要生成预览卡片 —— 你要确认卡片上显示的是 `Doni — your business admin on WhatsApp`,而不是一串乱码或者空白。别人转发你网站的时候,看到的就是这张卡片,它比网站本身还重要。

> 如果预览卡片不对或者不出来:WhatsApp 缓存很顽固,在网址后面加个 `?v=2`(比如 `https://fctech.ng?v=2`)再发一次,就能强制它重新抓取。

---

## 一页速查

| 我想干嘛 | 怎么做 |
|---|---|
| 改网站文字 | 改 `/Users/mac/Documents/Claude/Projects/doni-site/index.html` |
| 让线上更新(A 路) | `git add -A` → `git commit -m "改了"` → `git push` |
| 让线上更新(B 路) | Cloudflare → 项目 → Deployments → Create deployment → 拖文件 |
| 看有没有部署成功 | Cloudflare → Workers & Pages → doni-site → Deployments |
| 加一个新域名指过来 | 加 AAAA `100::` 橙云 → Rules → Redirect Rules → 301 到主域名 |
| 改了没生效 | `Command + Shift + R` 强刷,还不行就 Caching → Purge Everything |
| 看有多少人访问 | Analytics & Logs → Web Analytics |
